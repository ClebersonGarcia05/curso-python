""" Interactive help = ajuda interativa, para chamar é só codar help() 

docstrings = É um comentário de bloco dentro da função que descreve para que a função serve e para que cada parametro 
serve

Parametros opcionais =  def somar (a=0, b=0, c=0) = colocar o parametro recebendo 0 deixa eles como parametros opcionais.


"""
print(input.__doc__)